/**
 * Legacy keyring bridge removed in favour of encrypted SQLite storage.
 * This module is retained to avoid import resolution errors; all previous
 * keyring helpers have been retired.
 */

export {}
