{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sql-minimal",
  "description": "Allow SQL plugin load/select for main window (read-only)",
  "windows": ["main"],
  "permissions": [
    "sql:allow-load",
    "sql:allow-select",
    "sql:allow-execute"
  ]
}
