{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "secrets-minimal",
  "description": "Allow custom keyring commands for credentials management",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
